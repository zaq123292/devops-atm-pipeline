# 作业 01 任务 6 DevOps 流水线 ATM 自助银行系统实验报告

## 一 项目目标
本项目实现 Flask ATM 教学系统，并提供自动化测试、Docker 构建、CI/CD 配置、健康检查和 Prometheus 指标配置，展示 Build、Test、Deploy、Monitor 的 DevOps 流程。

## 二 系统功能与安全设计
系统提供卡号和 PIN 登录、余额查询、取款、存款、行内转账和交易记录。演示账户为 `10000001 / 1234`。金额以“分”为内部整数单位保存，避免浮点误差；所有 SQL 查询采用参数化语句；会话访问由登录检查保护；取款同时检查余额与日限额。

## 三 CI/CD 与容器设计
GitHub Actions 在 main、develop 推送及 main 的 PR 时执行依赖安装、pytest、覆盖率报告、flake8 和 Docker 镜像构建。只有 main 分支进入受保护的部署作业。`Dockerfile` 采用多阶段构建，`docker-compose.yml` 定义 PostgreSQL、Web 和 Nginx 服务。真实部署凭据通过 GitHub Secrets 注入，未写入源码。

## 四 监控与运维
`/health` 返回服务状态、运行时间和数据库状态；安装 Prometheus Exporter 后暴露 `/metrics`；`monitoring/prometheus.yml` 配置抓取目标。告警规则覆盖 5xx 比例、响应时间、数据库连接、取款失败率和磁盘使用率，详情见 `docs/AlertRules.md`。

## 五 测试结果
本地自动测试执行结果为 `5 passed`，覆盖首页、登录成功与注入失败、未登录访问重定向、健康检查、取款、存款、转账和交易历史。CI 使用的 flake8 检查也已通过。

| 测试类别 | 覆盖内容 | 结果 |
|---|---|---|
| 页面与会话 | 首页、登录、未授权重定向 | 通过 |
| 业务逻辑 | 取款、余额、存款、转账 | 通过 |
| 安全 | 参数化登录查询、会话保护 | 通过 |
| 运维 | 健康检查 | 通过 |

## 六 局限与待提交材料
本项目当前使用 SQLite 作为本地开发实现；生产 PostgreSQL 的 ORM/驱动接入需结合部署环境进一步实现。实际 GitHub Actions 成功页、Docker 部署日志、Prometheus 页面和回滚演示截图必须在你配置远程仓库、Secrets 和 Docker 主机后取得，报告不把本地文档当作这些截图的替代品。
